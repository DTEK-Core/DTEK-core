import { PageSkeleton } from '@/components/shared/loading/page-skeleton';

export default function SettingsLoading() {
  return <PageSkeleton variant="settings" titleWidth={170} subtitleWidth={410} />;
}
